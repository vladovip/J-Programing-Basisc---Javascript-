
function solve([a,b]) 
{
    let a = Number(input.shift());
    let b = Number(input.shift());
    let area = a*b;
    console.log(area.toFixed(2));
    
  
}
solve(); 
  // the overal task is not completed